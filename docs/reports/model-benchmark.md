# Model benchmark report

Generated by `python scripts/benchmark_models.py`.

The checked-in manifest compares the intended FAST_PATH and DEEP_PATH candidates. The repository environment did not expose model weights, a vLLM/llama.cpp endpoint, or GPU access, so the generated smoke measurements are **not** a quality or clinical comparison and no winner is promoted automatically. Configure `PI_LLM_BASE_URL`, deploy the candidate models, rerun the command, then obtain human/clinical approval before changing production routing.

Promotion gates: safety evaluation, regression suite, latency p50/p95, output review, license review, two distinct clinical approvals, shadow/canary, and rollback plan.
